# This workflow executes untrusted pull-request code. Do not add secrets,
# environments, deployments, or access to privileged external resources.
name: PR-safe unit tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: unit-tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-tests:
    name: unit tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: configure test result path
        run: echo "CI_TEST_RESULTS_FILE=$RUNNER_TEMP/handelsrepublik-test-results.ndjson" >> "$GITHUB_ENV"
      - name: run unit test suite
        continue-on-error: true
        run: npm run test:ci:record -- unit.tests "Unit test suite" -- npm test
      - name: publish test result table
        if: always()
        run: npm run test:ci:summary -- "Unit test results"
