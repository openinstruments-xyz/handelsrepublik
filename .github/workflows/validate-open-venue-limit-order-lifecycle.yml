name: "Live: submit, replace, and cancel a limit order"

on:
  push:
    branches: [main]
  schedule:
    - cron: "30 11 * * 1-5"
      timezone: Europe/Berlin
  workflow_dispatch:
    inputs:
      confirm_order_request:
        description: 'I understand this sends, replaces, and cancels real deeply non-marketable limit orders.'
        required: true
        type: boolean
        default: false
  workflow_call:

permissions:
  contents: read

concurrency:
  group: live-integration-tests-main
  queue: max
  cancel-in-progress: false

jobs:
  time-gate:
    name: require weekdays 07:00–22:40 Europe/Berlin
    if: >-
      (github.event_name == 'merge_group' || github.ref == 'refs/heads/main') &&
      (github.event_name != 'workflow_dispatch' ||
        (github.actor == 'VIEWVIEWVIEW' && inputs.confirm_order_request))
    runs-on: ubuntu-latest
    outputs:
      eligible: ${{ steps.clock.outputs.eligible }}
    steps:
      - id: clock
        name: validate open-market time window
        env:
          TZ: Europe/Berlin
        run: |
          weekday=$(date +%u)
          minutes=$((10#$(date +%H) * 60 + 10#$(date +%M)))
          if (( weekday <= 5 && minutes >= 420 && minutes < 1360 )); then
            echo "eligible=true" >> "$GITHUB_OUTPUT"
            result="✅ Passed"
            note="Inside weekdays 07:00–22:40 Europe/Berlin; the lifecycle test will run."
          else
            echo "eligible=false" >> "$GITHUB_OUTPUT"
            echo "Skipping outside weekdays 07:00–22:40 Europe/Berlin."
            result="⏭️ Skipped"
            note="Outside weekdays 07:00–22:40 Europe/Berlin."
          fi
          {
            echo "## Open-market time gate"
            echo ""
            echo "| Test | Result | Notes |"
            echo "|---|:---:|---|"
            echo "| Open-market eligibility | $result | $note |"
          } >> "$GITHUB_STEP_SUMMARY"

  open-limit-order:
    name: submit, replace, and cancel a limit order
    needs: time-gate
    if: needs.time-gate.outputs.eligible == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment: Live Integration Tests
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      - run: npm ci
      - name: configure ephemeral session path
        run: |
          echo "TR_SESSION_FILE=$RUNNER_TEMP/trade-republic-session.json" >> "$GITHUB_ENV"
          echo "CI_TEST_RESULTS_FILE=$RUNNER_TEMP/handelsrepublik-test-results.ndjson" >> "$GITHUB_ENV"
      - name: materialize encrypted test session
        env:
          TR_SESSION_JSON: ${{ secrets.TR_SESSION_JSON }}
        run: node --import tsx scripts/ci-session.ts materialize "$TR_SESSION_FILE"
      - name: refresh Trade Republic session
        id: live-ready
        run: node --import tsx scripts/ci-session.ts refresh "$TR_SESSION_FILE"
      - name: submit, replace, and cancel a limit order
        continue-on-error: true
        run: npm run test:ci:record -- orders.open-limit-lifecycle "Submit, replace, and cancel a limit order" -- npm run test:integration:open-limit-order
      - name: rotate encrypted GitHub session secret
        if: ${{ always() && steps.live-ready.outcome == 'success' }}
        env:
          GH_TOKEN: ${{ secrets.GH_CLI_TOKEN_USED_TO_UPDATE_TR_SESSION }}
        run: gh secret set TR_SESSION_JSON --repo "$GITHUB_REPOSITORY" < "$TR_SESSION_FILE"
      - name: remove materialized session
        if: always()
        run: rm -f "$TR_SESSION_FILE"
      - name: publish test result table
        if: always()
        run: npm run test:ci:summary -- "Submit, replace, and cancel a limit order"
