name: "Live: submit and cancel a weekend limit order"

on:
  push:
    branches: [main]
  schedule:
    - cron: "45 11 * * 6,0"
      timezone: Europe/Berlin
  workflow_dispatch:
    inputs:
      confirm_order_request:
        description: 'I understand this sends and cancels a real deeply non-marketable weekend limit order.'
        required: true
        type: boolean
        default: false
  workflow_call:

permissions:
  contents: read

concurrency:
  group: live-integration-tests-main
  queue: max
  cancel-in-progress: false

jobs:
  time-gate:
    name: require Saturday or Sunday in Europe/Berlin
    if: >-
      (github.event_name == 'merge_group' || github.ref == 'refs/heads/main') &&
      (github.event_name != 'workflow_dispatch' ||
        (github.actor == 'VIEWVIEWVIEW' && inputs.confirm_order_request))
    runs-on: ubuntu-latest
    outputs:
      eligible: ${{ steps.clock.outputs.eligible }}
    steps:
      - id: clock
        name: validate weekend time window
        env:
          TZ: Europe/Berlin
        run: |
          weekday=$(date +%u)
          if (( weekday >= 6 )); then
            echo "eligible=true" >> "$GITHUB_OUTPUT"
            result="✅ Passed"
            note="It is Saturday or Sunday in Europe/Berlin; the weekend lifecycle test will run."
          else
            echo "eligible=false" >> "$GITHUB_OUTPUT"
            echo "Skipping outside Saturday and Sunday in Europe/Berlin."
            result="⏭️ Skipped"
            note="It is not Saturday or Sunday in Europe/Berlin."
          fi
          {
            echo "## Weekend time gate"
            echo ""
            echo "| Test | Result | Notes |"
            echo "|---|:---:|---|"
            echo "| Weekend eligibility | $result | $note |"
          } >> "$GITHUB_STEP_SUMMARY"

  weekend-limit-order:
    name: submit and cancel a weekend limit order
    needs: time-gate
    if: needs.time-gate.outputs.eligible == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    environment: Live Integration Tests
    steps:
      - uses: actions/checkout@v6
        with:
          persist-credentials: false
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          package-manager-cache: false
      - run: npm ci
      - name: configure ephemeral session path
        run: |
          echo "TR_SESSION_FILE=$RUNNER_TEMP/trade-republic-session.json" >> "$GITHUB_ENV"
          echo "CI_TEST_RESULTS_FILE=$RUNNER_TEMP/handelsrepublik-test-results.ndjson" >> "$GITHUB_ENV"
      - name: materialize encrypted test session
        env:
          TR_SESSION_JSON: ${{ secrets.TR_SESSION_JSON }}
        run: node --import tsx scripts/ci-session.ts materialize "$TR_SESSION_FILE"
      - name: refresh Trade Republic session
        id: live-ready
        run: node --import tsx scripts/ci-session.ts refresh "$TR_SESSION_FILE"
      - name: submit and cancel a weekend limit order
        continue-on-error: true
        run: npm run test:ci:record -- orders.weekend-limit-lifecycle "Submit and cancel a weekend limit order" -- npm run test:integration:weekend-limit-order
      - name: rotate encrypted GitHub session secret
        if: ${{ always() && steps.live-ready.outcome == 'success' }}
        env:
          GH_TOKEN: ${{ secrets.GH_CLI_TOKEN_USED_TO_UPDATE_TR_SESSION }}
        run: gh secret set TR_SESSION_JSON --repo "$GITHUB_REPOSITORY" < "$TR_SESSION_FILE"
      - name: remove materialized session
        if: always()
        run: rm -f "$TR_SESSION_FILE"
      - name: publish test result table
        if: always()
        run: npm run test:ci:summary -- "Submit and cancel a weekend limit order"
